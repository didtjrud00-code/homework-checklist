rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /checklists/{document} {
      allow read, write: if request.auth != null
        && request.auth.token.email == "didtjrud00@gmail.com";
    }
  }
}
